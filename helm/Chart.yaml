apiVersion: v2
name: scheduling-report-worker
description: Scheduling Report Core - Python Kafka Consumer

type: application

# This is the chart version
version: 0.1.0

# This is the version number of the application being deployed
# This will be replaced by CI/CD pipeline with actual version
AppVersion: J_APP_VERSION
