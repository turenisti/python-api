# SonarQube Configuration for Scheduling Report Worker (Python API)
sonar.projectKey=Scheduling-Report-Core
sonar.projectName=Scheduling Report Core
sonar.projectVersion=1.0
sonar.qualitygate.wait=true

# Source code location
sonar.sources=execution_engine,shared
sonar.sourceEncoding=UTF-8

# Python specific
sonar.language=py
sonar.python.version=3.11

# Exclusions
sonar.exclusions=**/*_test.py,**/__pycache__/**,**/venv/**,**/.env,Dockerfile,helm/**

# Coverage (if you add tests later)
# sonar.python.coverage.reportPaths=coverage.xml
